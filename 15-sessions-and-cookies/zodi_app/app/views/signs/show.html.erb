<% if session[:view_count] <= 3 %>
  <h1>YOU HAVE <%= 3 - session[:view_count] %> Page Views Left</h1>

  <h1>Show Page</h1>

  <h1><%= @sign.name %></h1>
  <p><%= @sign.date %></p>
  <p><%= @sign.element %></p>

  <h3>All Users With Sign</h3>

  <% @sign.users.each do |user|  %>
    <p><%= user.name %></p>
  <% end %>


  <%= link_to "INDEX", signs_path %>
  <br>
                      <%# /signs/:id/edit %>
  <%= link_to "EDIT",  edit_sign_path(@sign) %>

                        <%# /signs/:id %>
  <%= button_to "DELETE", sign_path(@sign), method: :delete %>
<% else %>

  <h1>SHOW ADD</h1>
  <iframe width="946" height="532" src="https://www.youtube.com/embed/lHdLRrUocOA" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
  <%= button_to "I SUPER PROMISE I WATCHED THIS AD", reset_session_path, method: :patch %>

<% end %>



